

#今日收获

### 1. UTF-8 环境配置 ✅
- VS Code、终端、Git 全部改为 UTF-8
- 彻底解决中文乱码问题

---

### 2. scanf 与 printf
- `scanf`：读取数据
- `printf`：输出数据
- `%.2f`：输出时保留两位小数
- `%f`：读取 float 类型数据

示例：

```c
scanf("%f", &skill);
printf("%.2f", skill);
```

---

### 3. 编译习惯

以后统一使用：

```bash
gcc main.c -o main && ./main
```

避免运行旧程序。

---

### 4. 读取带空格的字符串

普通写法：

```c
scanf("%s", name);
```

只能读取第一个单词。

正确写法：

```c
scanf("%[^\n]%*c", name);
```

可以读取整行内容。

---

## 今日总结

今天解决了开发环境和输入输出的基础问题，之后可以专注学习 C 语言语法，不再被乱码和运行旧程序等问题困扰。